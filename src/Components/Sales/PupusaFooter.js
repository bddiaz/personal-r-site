import React from "react";
import styled from "styled-components";

function PupusaFooter(props) {
  return (
    <FooterContainer>
      The footer is this one. we neeed some more information here to place like
      links, etc.
    </FooterContainer>
  );
}
export default PupusaFooter;

const FooterContainer = styled.div`
  height: 15vh;
  width: 100%;
  background-color: #ddd7c4;
`;
